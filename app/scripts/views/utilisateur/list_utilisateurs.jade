.container
  .page-header
    h3 Utilisateurs
  .list-group
    .row
      .col-xs-8
        label(for='search') Filtre :
        input.form-control.search-field(ng-model='filterField', placeholder='texte')
      .col-xs-4
        label(for='search') Nb de Résultats par page :
        input.form-control.max-results-field(type='number', ng-model='lookup.max_results')
  list-resource-directive(resource-backend='resourceBackend', lookup='lookup')
    a(ng-href='#/utilisateurs/{{resource._id}}')
      | {{resource.pseudo}}
      span.label.label-primary.pull-right(ng-show="resource.role == 'Administrateur'")
        | Administrateur
      span.label.label-info.pull-right(ng-show="resource.role == 'Validateur'")
        | Validateur
